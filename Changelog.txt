### Added
- Support CUI command
- Automatically set Submit target to the previous application when opening the app without a hotkey if unset.
- Auto-save unsaved prompt to History when opening a file via CLI.

### Fixed
- Improved detection of the target application when opening PromptTap via the Dock or non-hotkey methods.
